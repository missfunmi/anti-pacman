pub struct Player {
    pub avatar: String,
    pub x: usize,
    pub y: usize,
    pub speed: f32,
}

pub struct PacPerson {
    pub avatar: String,
    pub x: usize,
    pub y: usize,
    pub speed: f32,
}
